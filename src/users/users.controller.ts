import { Controller } from '@nestjs/common';
import { UsersService } from './users.service';

@Controller('users')
export class UsersController {
  // constructor(private readonly _userService: UsersService) {}
  //   async getAllUsers() {
  //       try {
  //           const res = await this._userService.
  //       }
  //       catch (e) { }
  // }
}
